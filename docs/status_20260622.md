# Status 2026-06-22

## Completed

- Cloned upstream DMD2:
  `/vepfs-cnbja62d5d769987/suntengjiao/distill/dmd2`
- Added non-interactive official smoke script:
  `/vepfs-cnbja62d5d769987/suntengjiao/distill/dmd2/scripts/smoke_sdxl_lora_diffusers.py`
- Downloaded official smoke assets:
  - SDXL fp16 subset: `/vepfs-cnbja62d5d769987/suntengjiao/distill/dmd2_assets/sdxl-base-1.0-fp16`
  - DMD2 4-step LoRA: `/vepfs-cnbja62d5d769987/suntengjiao/distill/dmd2_assets/DMD2/dmd2_sdxl_4step_lora_fp16.safetensors`
- Ran official DMD2 SDXL 4-step LoRA smoke successfully in `twin_flow_qwen`.
- Created this migration harness.
- Implemented local FireRed/QwenImageEdit DMD2 LoRA dryrun:
  - `scripts/train_firered_dmd2_local.py`
  - `src/dmd2_firered/local_firered_data.py`
- Ran a real 3-step A800 dryrun on `gz-az7-gpu-a800-02-train-plt-server`.

## Smoke Result

Image:

`artifacts/official_sdxl_smoke/dmd2_sdxl_lora_4step_smoke.png`

Manifest:

`artifacts/official_sdxl_smoke/manifest.json`

## Environment Finding

`red_train` is not suitable for upstream DMD2 diffusers smoke because importing diffusers triggers the installed `flash_attn` binary and fails with `GLIBC_2.32 not found`.

`twin_flow_qwen` works for the official diffusers smoke:

- torch: `2.6.0+cu124`
- diffusers: `0.36.0`
- transformers: `4.57.3`

## FireRed Preflight Finding

Default 8-record preflight passes after mapping JSONL COS keys through:

`/vepfs-cnbja62d5d769987/suntengjiao/distill/firered_gray_depth/cos_data_clay`

But `--sample-check-count 128` fails starting at row 20. The current local mirror is only enough for a small dryrun/fastrun subset, not full training.

## FireRed Local DMD2 Dryrun

Passing run:

`/vepfs-cnbja62d5d769987/suntengjiao/distill/firered_gray_depth/outputs/dmd2_firered_gray_lora_smoke/local_3step_20260622_072159`

Contact sheet:

`/vepfs-cnbja62d5d769987/suntengjiao/distill/firered_gray_depth/outputs/dmd2_firered_gray_lora_smoke/local_3step_20260622_072159/eval/global_step_000003/contact_sheet.png`

Checkpoint:

`/vepfs-cnbja62d5d769987/suntengjiao/distill/firered_gray_depth/outputs/dmd2_firered_gray_lora_smoke/local_3step_20260622_072159/checkpoints/global_step_000003`

The run used one A800, bf16, TF32, fused AdamW, gradient checkpointing, `CFG=0`, and a 20-record fully local subset. Peak reserved GPU memory was about 50GiB.

The DM loss was zero on step 1 because student/fake/teacher all start from the gray LoRA. It became non-zero on steps 2 and 3, confirming that fake-critic updates are affecting the DMD2 score-difference path.

After cleanup of failed debug outputs, the valid 3-step dryrun occupies about 3.6GiB.

QA eval now follows the agreed comparison format:

```text
input | orig_lora_vanilla_40 | orig_lora_few_1nfe | dmd2_1nfe | dmd2_2nfe | dmd2_4nfe | target
```

One-sample QA eval smoke passed:

`/vepfs-cnbja62d5d769987/suntengjiao/distill/firered_gray_depth/outputs/dmd2_firered_gray_lora_smoke/local_3step_20260622_072159/offline_eval_qa_test/global_step_000003/contact_sheet.png`

## Next Required Step

For a longer local fastrun, use:

```bash
PY=/vepfs-cnbja62d5d769987/suntengjiao/anaconda3/envs/twin_flow_qwen/bin/python
$PY scripts/train_firered_dmd2_local.py \
  --config configs/firered_gray_dmd2_lora_smoke.yaml \
  --steps 100 \
  --device cuda \
  --eval-samples 4
```

Full training still requires full data localization or a deliberate streaming design.

Slurm fastrun wrapper:

`scripts/sbatch_firered_dmd2_lora_fastrun.sh`

Current long-run defaults:

- `DMD2_STEPS=20000`
- save checkpoint every `500` steps
- keep latest `5` checkpoints
- save optimizer/RNG state for resume
- walltime `5-00:00:00`
